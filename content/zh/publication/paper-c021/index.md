---
title: "Data imputation for sparse radio maps in indoor positioning"
authors:
  - "Xiao Li"
  - "李环"
  - "Harry Kai-Ho Chan"
  - "Hua Lu"
  - "Christian S. Jensen"
date: "2023-01-01"
publishDate: "2023-01-01"
publication_types:
  - "paper-conference"
publication: "The 39th IEEE International Conference on Data Engineering (ICDE)"
slug: "paper-c021"
abstract: "Indoor location-based services rely on the avail- (AP) [31], and is an integer value in the range of [−99, 0] ability of sufficiently accurate positioning in indoor spaces. A dBm. An example radio map is shown in the top-left part popular approach to positioning relies on so-called radio maps of Fig. 1. The online phase localizes the users by utilizing a that contain pairs of a vector of Wi-Fi signal strength indicator values (RSSIs), called a fingerprint, and a location label, called location estimation algorithm (e.g., KNN [57]) that compares a reference point (RP), in which the fingerprint was observed. the user device’s fingerprint with the radio map. The positioning accuracy depends on the quality of the radio maps and their fingerprints. Radio maps are often sparse, with Offline Phase many pairs containing vectors missing many RSSIs as well Data Imputation as RPs. Aiming to improve positioning accuracy, we present Sparse Radio Map Imputed Radio Map a complete set of techniques to impute such missing values Fingerprint RP Fingerprint RP in radio maps. We differentiate two types of missing RSSIs: (null, -68, null, …, -76) (124, 53) (-75, -68, -100, …, -76) (124, 53) missing not at random (MNAR) and missing at random (MAR). (-60, null, null, …, null) null (-60, -86, -100, …, -75) (117, 49) Specifically, we design a framework encompassing a missing RSSI ⋮ ⋮ ⋮ ⋮ differentiator followed by a data imputer for missing values. The (null, -56, null, …, null) null (-78, -56, -84, …, -87) (161, 68) differentiator identifies MARs and MNARs via clustering-based (null, null, null, …, -78) (144, 69) (-82, -48, -87, …, -78) (144, 69) fingerprint analysis. Missing RSSIs and RPs are then imputed jointly by means of a novel encoder-decoder architecture that Online Phase leverages temporal dependencies in data collection as well as Radio Map Creation Location online fingerprint correlations among fingerprints and RPs. A time-lag mechanism Estimation is used to consider the aging of data, and a sparsity-friendly Signal Collection attention mechanism is used to focus attention score calculation estimated location on observed data. Extensive experiments with real data from two Fig. 1: Fingerprinting procedure and radio maps. buildings show that our proposal outperforms the alternatives with significant advantages in terms of imputation accuracy and To build radio maps efficiently and economically for an indoor positioning accuracy. indoor space, surveys are often performed by surveyors mov- I. I NTRODUCTION ing in the indoor space [12], [24], [31], [37], [54]. Surveyors collect RSSIs continuously while moving along predefined Indoor applications involving navigation, augmented reality, paths, as illustrated in Fig. 2. Due to fluctuation in the wireless and moving robots require sufficiently accurate indoor posi- environment and asynchrony between the collection and RPs tioning. According to Research and Markets, the global indoor (to be detailed in Section II-B), the results of walking surveys positioning and navigation market will exceed $54 billion by 2026 [1]. While a variety of indoor positioning technolo- (x8, y8) t8 reference point (RP) gies exist, positioning based on Wi-Fi fingerprinting [26] is t7 survey path popular: the ubiquity of Wi-Fi enables positioning without t6 room t5 the deployment of additional expensive infrastructure, and the t1 t2 t3 t4 (x5, y5) hallway technology is non-intrusive to users. However, the accuracy of (x1, y1) collection point Wi-Fi fingerprinting based positioning depends heavily on the without an RP quality of the radio map data used [38], [46], [52], [55]. Wi-Fi fingerprinting entails two phases, as shown in Fig. 1. Fig. 2: Walking survey based data collection. The offline phase creates a so-called radio map that contains pairs of a vector of Wi-Fi received signal strength indicator suffer from low data quality, having high rates of missing (i.e., values (RSSIs), called a fingerprint, and a location label, called percentages of nulls) RSSIs and RPs in a radio map. For a reference point (RP), in which the fingerprint was observed. example, in the radio maps obtained from walking surveys in An RSSI measures the signal strength of a Wi-Fi access point two real buildings called Kaide and Wanda (to be detailed in Section V-A), the rates of missing RSSIs and RPs are between MNARs, while the latter treat them as MARs. We will offer 85.6% and 93.7%. In other words, the radio maps are highly empirical evidence of the benefits of differentiation. sparse, having many nulls. Such nulls must be replaced To differentiate MNARs and MARs, we design a clustering by real numbers in order for the radio map to be used by based differentiator that clusters a radio map’s fingerprints, location estimation algorithms [32], [49]. Intuitively, imputing and determines MARs and MNARs via intra-cluster analyses. accurate real numbers for nulls in a radio map improves To obtain appropriate clusters, we design two clustering al- its usability in indoor positioning. However, existing studies gorithms that utilize a specialized accuracy metric and indoor employ straightforward strategies to fill-in RP nulls [18], topology, respectively. [21], [23] and RSSI nulls [32], [37], [49], yielding subpar The subsequent imputation of missing values is also chal- results (cf. Section V-C). Therefore, this study focuses on lenging. Following existing radio map completion studies [23], improving the quality and usability of sparse radio maps by [37], [45], we replace identified MNAR values by the value accurately imputing missing RSSI and RP data. In doing so, −100 dBm, the lowest RSSI value that thus reflects the the study contends with difficult challenges. unobservability of MNARs. However, imputing MARs and First, two types of missing RSSIs exist. Missing Not At missing RPs is not straightforward. A MAR should be im- Random (MNAR) RSSIs are caused by the unobservability puted with a value in [−99, 0] dBm,2 since its value would of the signals of APs. This typically occurs when an AP have been observed had the random event that caused it not is too far away and cannot be seen by a user’s device. In occurred. Traditional radio map completion methods impute contrast, Missing At Random (MAR) RSSIs1 result from missing RPs using linear interpolation [37] or semi-supervised random events, e.g., the temporary presence of obstacles in learning [49]. General data imputation methods employ matrix transmission paths or occasional loss of contact with APs [18], factorization [25] or chained equations [6]. However, all these [21]. methods fall short when data sparsity is high, and they do not An example of MAR RSSI and MNAR RSSI is shown in consider the correlations and temporal dependencies between Fig. 3. An AP (access point) is selected in each venue and RSSIs and RPs collected along a path in walking-survey based its deployment location is roughly within the dashed circle. radio map data collection. Next, time-series imputation meth- For an RP (reference point), if all fingerprints collected at that ods [10], [11], [13], [17], [44], [56] target missing values in RP have observed the selected AP, the RP is marked in red; feature or source sequences (e.g., multivariate time series) with otherwise, some of its fingerprints have missed the selected known labels. In contrast, we must contend with heterogeneous AP, and that RP is marked in blue. Clearly, most RPs far away missing values-MARs in source (or fingerprint) sequences from the selected AP are blue, indicating that the selected AP and missing RPs in target (or RP) sequences. is unobservable at these RPs and the corresponding missing To impute missing MARs and RPs effectively for sparse events are classified as Missing Not At Random (MNAR). On radio maps, we design an encoder-decoder based data imputer the other hand, most of the RPs near the dashed circle are red that exploits both temporal dependencies in time series and but there are several blue RPs that sometimes miss the selected correlations between source and target sequences to impute AP’s signals. The missing events in these RPs are incidental MARs in source sequences and missing RPs in target se- and should be treated as Missing At Random (MAR). quences jointly. A standard encoder-decoder is unsuitable in this setting, due to the many missing values in both source room (fingerprint) and target (RP) sequences. The irregularity in hallway RP with missing RSSI the absence of RSSIs results in different durations between RP with observed RSSI consecutive encoder units (see Table IV in Section IV-B), which a standard encoder-decoder cannot handle. Further, missing values in the input also degrades the ability of the room model’s attention mechanism at capturing the importance of hallway RP with missing RSSI each unit. To tackle these issues, we introduce a time-lag RP with observed RSSI mechanism that considers the aging of the last observed Fig. 3: Observability of a selected AP’s signals at different value when modeling the relationships between consecutive reference points (RPs). encoder units, and we design an adapted attention mechanism to contend with the high sparsity of input features. As the two types of missing RSSIs have different causes We make the following major contributions. and meanings, they should be differentiated before imputa- • We differentiate missing RSSIs as MARs and MNARs. tion [47]. However, neither traditional radio map completion To the best of our knowledge, we are the first to do methods [23], [37], [45] nor general data imputers [6], [10], so. Specifically, we provide a clustering-based approach to [11], [13], [17], [25], [44], [56] differentiate the missing RSSI differentiate missing RSSIs (Section III). types. The former simply assume that all missing RSSIs are • We devise a novel encoder-decoder that is capable of imput- ing missing RSSIs and RPs jointly by exploiting temporal 1 The terms MNAR and MAR stem from literature [39], [47]. They are applied to missing RSSI values in this work. 2 −99 dBm −100 dBm in terms of power as dBm is log-based [2]. 2"
keywords:
  - "data"
  - "imputation"
  - "sparse"
  - "radio"
  - "maps"
  - "indoor"
  - "positioning"
doi: "10.1109/icde55515.2023.00173"
aliases:
  - "/zh/publication/dblp-conficde-li-0-c-0-j-23/"
links:
  - name: "DBLP"
    url: "https://dblp.org/rec/conf/icde/Li0C0J23"
  - name: "arXiv"
    url: "https://arxiv.org/abs/2302.13022"
  - name: "Project"
    url: "https://longaspire.github.io/publication/"
---
